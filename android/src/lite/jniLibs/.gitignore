# .so files are generated by CI (precompile_binaries workflow with build_lite=true)
# or by scripts/build_lite_android.sh.
# They are NOT committed to the repo; cargokit downloads them at build time
# via the precompiled_lite_<hash> release tag.
*.so
